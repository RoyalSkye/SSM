<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.mapper.FreelistenbookMapper">                                                                                                                               


	
	
	<select id="findAllFreelistenbook"  resultType="com.neusoft.po.Freelistenbook">
		select * from Freelistenbook
	</select>
	
	
	
	<select id="findFreelistenbook"  parameterType="FreelistenbookVo" resultType="com.neusoft.po.Freelistenbook">
		select Freelistenbook.*
		from
		on Freelistenbook.id=#{id}
		where 1=1
			
		<if test="cname != null and cname!=''">
			and cname like '%${cname}%'
		</if>
		<if test="status != null and status !=''">
			and status = #{status}
		</if>
		<if test="beginDate != null and beginDate!='' ">
			and booktime &gt;='%${beginDate}%'
		</if>
		<if test="endDate != null and endDate!='' ">
			and booktime &lt;= '%${endDate}%'
		</if>		
	</select>
	
	
	<update id="updateFreelistenbook" parameterType="int">
			update Freelistenbook set status='已处理' where id=#{id}
	
	</update>
	
	
	
</mapper>